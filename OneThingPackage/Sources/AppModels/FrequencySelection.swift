public enum FrequencySelection: String, CaseIterable, Hashable {
  case never = "never"
  case daily = "daily"
  case weekly = "weekly"
  case monthly = "monthly"
  case yearly = "yearly"
  case custom = "custom"
}
